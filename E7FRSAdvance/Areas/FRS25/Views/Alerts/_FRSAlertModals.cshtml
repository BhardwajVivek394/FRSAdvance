@* ═══════════════════════════════════════════════════════════════
    FRS ALERT MODALS
    Include once: @Html.Partial("_FRSAlertModals")
    Requires: Bootstrap 4 modal JS + FontAwesome
    ═══════════════════════════════════════════════════════════════ *@

<style>
    .frs-modal .modal-content {
        border: none;
        border-radius: 14px;
        overflow: hidden;
        box-shadow: 0 12px 40px rgba(0,0,0,.38);
        background: #1e293b;
    }

    .frs-modal .modal-header {
        background: #0f172a;
        color: #f1f5f9;
        padding: 13px 20px;
        border-bottom: 1px solid #334155;
        display: flex;
        align-items: center;
        justify-content: space-between;
    }

    .frs-modal .modal-title {
        font-size: 13.5px;
        font-weight: 600;
        color: #f1f5f9;
        display: flex;
        align-items: center;
        gap: 8px;
        margin: 0;
    }

        .frs-modal .modal-title i {
            font-size: 13px;
            color: #7dd3fc;
        }

    .frs-modal .btn-close-frs {
        background: none;
        border: none;
        color: #64748b;
        font-size: 16px;
        cursor: pointer;
        padding: 2px 6px;
        border-radius: 6px;
        transition: background .15s, color .15s;
        line-height: 1;
    }

        .frs-modal .btn-close-frs:hover {
            background: #1e3a5f;
            color: #f1f5f9;
        }

    .frs-modal .modal-body {
        padding: 20px 22px 16px;
        background: #1e293b;
    }

    .frs-modal .modal-footer {
        padding: 11px 22px;
        background: #0f172a;
        border-top: 1px solid #334155;
        display: flex;
        justify-content: flex-end;
        gap: 8px;
    }

    .frs-modal .frs-label {
        display: block;
        font-size: 11px;
        font-weight: 700;
        color: #94a3b8;
        text-transform: uppercase;
        letter-spacing: .07em;
        margin-bottom: 6px;
    }

        .frs-modal .frs-label .req {
            color: #f87171;
            margin-left: 2px;
        }

    .frs-modal .frs-input,
    .frs-modal .frs-select,
    .frs-modal .frs-textarea {
        width: 100%;
        background: #0f172a;
        border: 1px solid #334155;
        border-radius: 8px;
        padding: 9px 12px;
        font-size: 13px;
        color: #e2e8f0;
        outline: none;
        box-sizing: border-box;
        transition: border-color .15s, box-shadow .15s;
        font-family: inherit;
        color-scheme: dark;
    }

        .frs-modal .frs-input::placeholder,
        .frs-modal .frs-textarea::placeholder {
            color: #475569;
        }

        .frs-modal .frs-input:focus,
        .frs-modal .frs-select:focus,
        .frs-modal .frs-textarea:focus {
            border-color: #3b82f6;
            box-shadow: 0 0 0 3px rgba(59,130,246,.18);
        }

        .frs-modal .frs-select option {
            background: #1e293b;
            color: #e2e8f0;
        }

    .frs-modal .frs-textarea {
        resize: vertical;
        min-height: 90px;
    }

    .frs-modal .frs-field {
        margin-bottom: 14px;
    }

        .frs-modal .frs-field:last-child {
            margin-bottom: 0;
        }
    /* pill legend strip */
    .frs-pills-strip {
        display: flex;
        gap: 6px;
        flex-wrap: wrap;
        margin-bottom: 16px;
        padding: 10px 12px;
        background: #0f172a;
        border: 1px solid #334155;
        border-radius: 10px;
    }

    .frs-pill {
        display: inline-flex;
        align-items: center;
        gap: 5px;
        padding: 4px 11px;
        border-radius: 20px;
        font-size: 11.5px;
        font-weight: 700;
        white-space: nowrap;
    }

        .frs-pill .dot {
            width: 6px;
            height: 6px;
            border-radius: 50%;
            display: inline-block;
        }

    .frs-pill-pt {
        background: #422006;
        color: #fbbf24;
        border: 1px solid #78350f;
    }

        .frs-pill-pt .dot {
            background: #fbbf24;
        }

    .frs-pill-t {
        background: #052e16;
        color: #4ade80;
        border: 1px solid #166534;
    }

        .frs-pill-t .dot {
            background: #4ade80;
        }

    .frs-pill-f {
        background: #3b0707;
        color: #f87171;
        border: 1px solid #7f1d1d;
    }

        .frs-pill-f .dot {
            background: #f87171;
        }

    .frs-pill-m {
        background: #1c1917;
        color: #a78bfa;
        border: 1px solid #4c1d95;
    }

        .frs-pill-m .dot {
            background: #a78bfa;
        }
    /* buttons */
    .frs-btn {
        display: inline-flex;
        align-items: center;
        gap: 6px;
        padding: 8px 18px;
        border-radius: 8px;
        font-size: 12.5px;
        font-weight: 600;
        border: none;
        cursor: pointer;
        transition: opacity .15s, transform .1s;
        font-family: inherit;
    }

        .frs-btn:active {
            transform: scale(.97);
        }

    .frs-btn-cancel {
        background: #1e293b;
        color: #94a3b8;
        border: 1px solid #334155;
    }

        .frs-btn-cancel:hover {
            background: #334155;
            color: #e2e8f0;
        }

    .frs-btn-save {
        background: #3b82f6;
        color: #fff;
    }

        .frs-btn-save:hover {
            background: #2563eb;
        }

    /* ── MODAL 3b: scope image modal to content area; no sidebar/panel bleed ── */
    #modalRemarkImage.modal {
        left: 230px;
        right: 340px;
        width: auto;
    }

    /* scoped dim — pseudo-element replaces Bootstrap backdrop for this modal */
    #modalRemarkImage.modal.show::before {
        content: '';
        position: fixed;
        top: 0; bottom: 0;
        left: 230px;
        right: 340px;
        background: rgba(0, 0, 0, 0.65);
        z-index: -1;
    }

    /* history panel stays above image modal */
    #modalFRSAlertRemark {
        z-index: 1065 !important;
    }

    /* hide Bootstrap's full-screen backdrop when image modal is open */
    body.frs-image-modal-open > .modal-backdrop {
        display: none !important;
    }

    /* ── MODAL 3: history legend strip ── */
    .frs-history-legend {
        display: flex;
        gap: 6px;
        flex-wrap: wrap;
        padding: 10px 20px;
        background: #0a0f1a;
        border-bottom: 1px solid #1e2d40;
    }

    /* ── MODAL 3: scrollable history body ── */
    .frs-history-body {
        max-height: 460px;
        overflow-y: auto;
        overflow-x: hidden;
        padding: 16px 18px;
        display: flex;
        flex-direction: column;
        gap: 10px;
        /* hide scrollbar track until hovered */
        scrollbar-width: thin;
        scrollbar-color: transparent transparent;
        transition: scrollbar-color .2s;
    }

        .frs-history-body:hover {
            scrollbar-color: #2d3f55 transparent;
        }

        .frs-history-body::-webkit-scrollbar { width: 4px; }
        .frs-history-body::-webkit-scrollbar-track { background: transparent; }
        .frs-history-body::-webkit-scrollbar-thumb {
            background: transparent;
            border-radius: 4px;
            transition: background .2s;
        }
        .frs-history-body:hover::-webkit-scrollbar-thumb { background: #2d3f55; }

    .frs-history-loading {
        text-align: center;
        padding: 40px 20px;
        color: #475569;
        font-size: 13px;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 10px;
    }

        .frs-history-loading i { font-size: 18px; color: #3b82f6; }

    /* ── MODAL 3: remark card — image-first layout ── */
    .frs-remark-card {
        background: #0f172a;
        border: 1px solid #1e2d40;
        border-radius: 12px;
        overflow: hidden;
        transition: border-color .18s, box-shadow .18s;
        display: flex;
        flex-direction: column;
    }

        .frs-remark-card:hover {
            border-color: #2d4a6a;
            box-shadow: 0 4px 20px rgba(0,0,0,.35);
        }

    /* top bar: seq + pill + meta */
    .frs-remark-card-head {
        display: flex;
        align-items: center;
        gap: 9px;
        padding: 10px 14px;
        background: #080e1a;
        border-bottom: 1px solid #1a2740;
    }

    .frs-remark-seq {
        width: 20px; height: 20px;
        background: #1e293b;
        border: 1px solid #334155;
        border-radius: 50%;
        font-size: 9.5px;
        font-weight: 700;
        color: #64748b;
        display: flex; align-items: center; justify-content: center;
        flex-shrink: 0;
    }

    .frs-remark-card-head .frs-pill { font-size: 10px; padding: 2px 8px; }

    .frs-remark-card-head-right {
        margin-left: auto;
        display: flex; align-items: center; gap: 12px;
    }

    .frs-remark-meta {
        display: flex; align-items: center; gap: 4px;
        font-size: 10.5px; color: #64748b; white-space: nowrap;
    }
        .frs-remark-meta i { font-size: 10px; color: #475569; }
        .frs-remark-meta strong { color: #94a3b8; font-weight: 600; }

    /* main row: image panel on left, content on right */
    .frs-remark-card-main {
        display: flex;
        gap: 0;
    }

    /* ── image panel ── */
    .frs-remark-img-col {
        flex-shrink: 0;
        width: 110px;
        min-height: 90px;
        background: #06090f;
        border-right: 1px solid #1a2740;
        display: flex;
        flex-direction: column;
        align-items: stretch;
        justify-content: stretch;
        position: relative;
        cursor: pointer;
        overflow: hidden;
        transition: width .22s cubic-bezier(.4,0,.2,1);
    }

        .frs-remark-img-col:hover:not(.no-image) { width: 130px; }

        .frs-remark-img-col.no-image {
            cursor: default;
            width: 80px;
        }

    /* full-bleed background preview */
    .frs-remark-img-bg {
        position: absolute;
        inset: 0;
        background-size: cover;
        background-position: center;
        filter: blur(6px) brightness(.45);
        transform: scale(1.08);
        transition: filter .25s, transform .25s;
    }

        .frs-remark-img-col:hover:not(.no-image) .frs-remark-img-bg {
            filter: blur(3px) brightness(.55);
            transform: scale(1.12);
        }

    /* sharp thumbnail on top of blurred bg */
    .frs-remark-img-thumb {
        position: relative;
        z-index: 1;
        margin: 10px auto 0;
        width: 76px;
        height: 60px;
        border-radius: 8px;
        overflow: hidden;
        border: 1.5px solid rgba(129,140,248,.35);
        box-shadow: 0 4px 16px rgba(0,0,0,.5);
        transition: transform .22s, border-color .22s, box-shadow .22s;
        flex-shrink: 0;
    }

        .frs-remark-img-col:hover:not(.no-image) .frs-remark-img-thumb {
            transform: translateY(-2px) scale(1.05);
            border-color: rgba(129,140,248,.75);
            box-shadow: 0 8px 24px rgba(0,0,0,.6), 0 0 0 3px rgba(129,140,248,.12);
        }

    .frs-remark-img-thumb img {
        width: 100%; height: 100%;
        object-fit: cover;
        display: block;
    }

    /* placeholder when no real thumb */
    .frs-remark-img-thumb-icon {
        width: 100%; height: 100%;
        display: flex; align-items: center; justify-content: center;
        background: #1a2740;
        font-size: 22px;
        color: #334155;
    }

    /* VIEW label + icon below thumb */
    .frs-remark-img-badge {
        position: relative;
        z-index: 1;
        margin: 6px auto 10px;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 4px;
        background: rgba(129,140,248,.13);
        border: 1px solid rgba(129,140,248,.25);
        border-radius: 20px;
        padding: 3px 10px;
        font-size: 9.5px;
        font-weight: 700;
        letter-spacing: .05em;
        color: #818cf8;
        text-transform: uppercase;
        transition: background .2s, border-color .2s, color .2s;
        white-space: nowrap;
    }

        .frs-remark-img-col:hover:not(.no-image) .frs-remark-img-badge {
            background: rgba(129,140,248,.28);
            border-color: rgba(129,140,248,.55);
            color: #c4b5fd;
        }

    /* ripple effect on click */
    .frs-remark-img-col .frs-ripple {
        position: absolute;
        border-radius: 50%;
        background: rgba(129,140,248,.35);
        transform: scale(0);
        animation: frsRipple .5s linear;
        pointer-events: none;
        z-index: 10;
    }

    @@keyframes frsRipple {
        to { transform: scale(4); opacity: 0; }
    }

    /* no-image state */
    .frs-remark-img-col.no-image .frs-remark-img-thumb {
        border-color: #1e2d40;
        opacity: .4;
    }

    .frs-remark-img-col.no-image .frs-remark-img-badge {
        color: #334155;
        border-color: #1e2d40;
        background: transparent;
    }

    /* content column */
    .frs-remark-content-col {
        flex: 1;
        padding: 12px 14px;
        display: flex;
        flex-direction: column;
        gap: 6px;
        min-width: 0;
    }

    .frs-remark-cause {
        display: inline-flex;
        align-items: center;
        gap: 5px;
        font-size: 10px;
        font-weight: 700;
        color: #60a5fa;
        background: rgba(59,130,246,.1);
        border: 1px solid rgba(59,130,246,.2);
        border-radius: 6px;
        padding: 2px 8px;
        width: fit-content;
    }

    .frs-remark-text {
        font-size: 12.5px;
        color: #cbd5e1;
        line-height: 1.6;
        word-break: break-word;
        margin: 0;
    }

    .frs-remark-card-foot {
        padding: 7px 14px;
        background: rgba(8,14,26,.6);
        border-top: 1px solid #1a2740;
        display: flex;
        align-items: center;
        gap: 14px;
    }

    .frs-remark-foot-item {
        display: flex; align-items: center; gap: 4px;
        font-size: 10.5px; color: #475569;
    }
        .frs-remark-foot-item i { font-size: 10px; }
        .frs-remark-foot-item span { color: #64748b; }

    /* ── MODAL 3: remarks history table (legacy fallback) ── */
    .frs-remark-table {
        width: 100%;
        border-collapse: collapse;
        font-size: 12.5px;
    }

        .frs-remark-table thead tr {
            background: #0f172a;
        }

        .frs-remark-table thead th {
            padding: 8px 12px;
            font-size: 10.5px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: .07em;
            color: #64748b;
            border-bottom: 1px solid #334155;
            white-space: nowrap;
        }

        .frs-remark-table tbody tr {
            border-bottom: 1px solid rgba(51,65,85,.5);
            transition: background .12s;
        }

        .frs-remark-table tbody tr:last-child {
            border-bottom: none;
        }

        .frs-remark-table tbody tr:hover {
            background: rgba(30,41,59,.6);
        }

        .frs-remark-table td {
            padding: 10px 12px;
            color: #cbd5e1;
            vertical-align: middle;
        }

        .frs-remark-table .td-date {
            font-weight: 600;
            color: #e2e8f0;
            white-space: nowrap;
        }

        .frs-remark-table .td-time {
            font-weight: 600;
            color: #e2e8f0;
            white-space: nowrap;
            font-variant-numeric: tabular-nums;
        }

        .frs-remark-table .td-img {
            text-align: center;
        }

    .frs-img-thumb-btn {
        background: rgba(99,102,241,.12);
        border: 1px solid rgba(99,102,241,.3);
        border-radius: 7px;
        color: #818cf8;
        font-size: 15px;
        width: 30px;
        height: 30px;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        cursor: pointer;
        transition: background .15s, border-color .15s, transform .1s;
        padding: 0;
    }

        .frs-img-thumb-btn:hover {
            background: rgba(99,102,241,.28);
            border-color: rgba(99,102,241,.55);
            transform: scale(1.08);
        }

    /* ── MODAL 3b: image viewer zoom ── */
    .frs-img-viewer-wrap {
        position: relative;
        background: #0a0f1a;
        border-radius: 8px;
        overflow: hidden;
        line-height: 0;
        user-select: none;
        cursor: zoom-in;
    }

        .frs-img-viewer-wrap.is-zoomed {
            cursor: grab;
        }

        .frs-img-viewer-wrap.is-dragging {
            cursor: grabbing;
        }

    #remarkImageEl {
        display: block;
        width: 100%;
        max-width: 100%;
        max-height: 580px;
        object-fit: contain;
        border-radius: 8px;
        border: 1px solid #334155;
        transform-origin: center center;
        transition: transform .2s ease;
        pointer-events: none;
    }

    .frs-zoom-controls {
        position: absolute;
        bottom: 10px;
        right: 10px;
        display: flex;
        align-items: center;
        gap: 4px;
        background: rgba(15,23,42,.85);
        border: 1px solid #334155;
        border-radius: 8px;
        padding: 4px 6px;
        backdrop-filter: blur(4px);
    }

    .frs-zoom-btn {
        background: #1e293b;
        border: 1px solid #334155;
        border-radius: 5px;
        color: #e2e8f0;
        font-size: 13px;
        width: 24px;
        height: 24px;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        cursor: pointer;
        transition: background .12s;
        padding: 0;
        line-height: 1;
    }

        .frs-zoom-btn:hover {
            background: #3b82f6;
            border-color: #3b82f6;
        }

    .frs-zoom-level {
        font-size: 11px;
        font-weight: 700;
        color: #94a3b8;
        min-width: 38px;
        text-align: center;
        font-variant-numeric: tabular-nums;
    }

    .frs-zoom-reset {
        background: #1e293b;
        border: 1px solid #334155;
        border-radius: 5px;
        color: #64748b;
        font-size: 10px;
        font-weight: 700;
        width: 24px;
        height: 24px;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        cursor: pointer;
        transition: background .12s, color .12s;
        padding: 0;
    }

        .frs-zoom-reset:hover {
            background: rgba(248,113,113,.15);
            border-color: rgba(248,113,113,.4);
            color: #f87171;
        }

    .frs-img-hint {
        text-align: center;
        font-size: 10.5px;
        color: #475569;
        margin-top: 8px;
        line-height: 1;
    }
</style>


@* ══════════════════════════════════════
    MODAL 1 — Add Feedback + Remark
    PT / T / F pill buttons call AddRemark()
    Save calls fnUpdateRemark()
    ══════════════════════════════════════ *@
<div class="modal fade frs-modal" id="modelAddRemark" tabindex="-1" role="dialog" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" style="max-width:490px;">
        <div class="modal-content">

            <div class="modal-header">
                <span class="modal-title">
                    <i class="fas fa-comment-medical"></i> Add Feedback &amp; Remark
                </span>
                <button type="button" class="btn-close-frs" data-dismiss="modal">
                    <i class="fas fa-times"></i>
                </button>
            </div>

            <div class="modal-body">

                <div class="frs-pills-strip">
                    <span class="frs-pill frs-pill-pt">
                        <span class="dot"></span>PT &ndash; Partially True
                    </span>
                    <span class="frs-pill frs-pill-t">
                        <span class="dot"></span>T &ndash; True
                    </span>
                    <span class="frs-pill frs-pill-f">
                        <span class="dot"></span>F &ndash; False
                    </span>
                    <span class="frs-pill frs-pill-m">
                        <span class="dot"></span>M &ndash; Maintenance
                    </span>
                </div>

                <input type="hidden" id="hdnAlertRemarkId" />
                <input type="hidden" id="hdnAlertAlertStatusId" />

                <div class="frs-field" id="divRemarkAlertInfo" style="display:none;">
                    <label class="frs-label">Cause Code <span class="req">*</span></label>
                    <select class="frs-select" id="drpRemarkAlertInfo">
                        <option value="">-- Select --</option>
                    </select>
                </div>

                <div class="frs-field">
                    <label class="frs-label">Remark <span class="req">*</span></label>
                    <textarea class="frs-textarea" id="txtAlertRemark" placeholder="Enter your remark here..."></textarea>
                </div>

                <div class="frs-field">
                    <label class="frs-label">Rectification Date &amp; Time</label>
                    <input type="datetime-local" class="frs-input" id="txtRectificationDateTime" />
                </div>

            </div>

            <div class="modal-footer">
                <button type="button" class="frs-btn frs-btn-cancel" data-dismiss="modal">
                    <i class="fas fa-times"></i> Cancel
                </button>
                <button type="button" class="frs-btn frs-btn-save" onclick="fnUpdateRemark()">
                    <i class="fas fa-save"></i> Save Remark
                </button>
            </div>

        </div>
    </div>
</div>


@* ══════════════════════════════════════
    MODAL 2 — Add Remark only
    "Add Remark" button calls AddRemarkWithoutFeedback()
    Save calls fnUpdateRemarkWithoutFeedback()
    ══════════════════════════════════════ *@
<div class="modal fade frs-modal" id="modelAddRemarkWithOutFeedback" tabindex="-1" role="dialog" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" style="max-width:450px;">
        <div class="modal-content">

            <div class="modal-header">
                <span class="modal-title">
                    <i class="fas fa-pencil-alt"></i> Add Remark
                </span>
                <button type="button" class="btn-close-frs" data-dismiss="modal">
                    <i class="fas fa-times"></i>
                </button>
            </div>

            <div class="modal-body">
                <input type="hidden" id="hdnAlertRemarkWithOutFeedbackId" />
                <input type="hidden" id="hdnAlertAlertStatusWithOutFeedbackId" />

                <div class="frs-field">
                    <label class="frs-label">Remark <span class="req">*</span></label>
                    <textarea class="frs-textarea" id="txtAlertRemarkWithOutFeedback" placeholder="Enter your remark here..."></textarea>
                </div>
            </div>

            <div class="modal-footer">
                <button type="button" class="frs-btn frs-btn-cancel" data-dismiss="modal">
                    <i class="fas fa-times"></i> Cancel
                </button>
                <button type="button" class="frs-btn frs-btn-save" onclick="fnUpdateRemarkWithoutFeedback()">
                    <i class="fas fa-save"></i> Save Remark
                </button>
            </div>

        </div>
    </div>
</div>


@* ══════════════════════════════════════
    MODAL 3 — View Feedback & Remarks history
    fnShowRemark(id) injects AJAX into #divFRSAlertRemark
    ══════════════════════════════════════ *@
@*<div class="modal fade frs-modal" id="modalFRSAlertRemark" tabindex="-1" role="dialog" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered" style="max-width:820px;">
            <div class="modal-content">

                <div class="modal-header">
                    <span class="modal-title">
                        <i class="fas fa-list-alt"></i> Feedback &amp; Remarks History
                    </span>
                    <button type="button" class="btn-close-frs" data-dismiss="modal">
                        <i class="fas fa-times"></i>
                    </button>
                </div>

                <div class="modal-body" style="padding:0;">
                    <div id="divFRSAlertRemark" style="padding:20px 22px;min-height:120px;">
                        <div style="text-align:center;padding:40px 20px;color:#475569;">
                            <i class="fas fa-spinner fa-spin" style="font-size:22px;display:block;margin-bottom:10px;color:#3b82f6;"></i>
                            Loading...
                        </div>
                    </div>
                </div>

                <div class="modal-footer">
                    <button type="button" class="frs-btn frs-btn-cancel" data-dismiss="modal">
                        <i class="fas fa-times"></i> Close
                    </button>
                </div>

            </div>
        </div>
    </div>*@



@* ══════════════════════════════════════
    MODAL 3 — View Feedback & Remarks history
    fnShowRemark(id) injects AJAX into #divFRSAlertRemark
    ══════════════════════════════════════ *@
<div class="modal fade frs-modal" id="modalFRSAlertRemark" tabindex="-1" role="dialog" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-lg">
        <div class="modal-content">

            <div class="modal-header">
                <span class="modal-title">
                    <i class="fas fa-list-alt"></i> Feedback &amp; Remarks History
                </span>
                <button type="button" class="btn-close-frs" data-dismiss="modal">
                    <i class="fas fa-times"></i>
                </button>
            </div>

            <div class="modal-body" style="padding:0;">
                <!-- pill legend -->
                <div class="frs-history-legend">
                    <span class="frs-pill frs-pill-pt"><span class="dot"></span>PT &ndash; Partially True</span>
                    <span class="frs-pill frs-pill-t"><span class="dot"></span>T &ndash; True</span>
                    <span class="frs-pill frs-pill-f"><span class="dot"></span>F &ndash; False</span>
                    <span class="frs-pill frs-pill-m"><span class="dot"></span>M &ndash; Maintenance</span>
                </div>
                <div id="divFRSAlertRemark" class="frs-history-body">
                    <div class="frs-history-loading">
                        <i class="fas fa-spinner fa-spin"></i>
                        Loading history...
                    </div>
                </div>
            </div>

            <div class="modal-footer">
                <button type="button" class="frs-btn frs-btn-cancel" data-dismiss="modal">
                    <i class="fas fa-times"></i> Close
                </button>
            </div>

        </div>
    </div>
</div>

@* ══════════════════════════════════════
    MODAL 3b — Remark Image Viewer
    Triggered by fnShowRemarkImage(remarkId)
    Fetches base64 image via api/FRSAlert/GetFRSRemarkBy/Id/{id}
    CHANGED: dialog widened to 780px; image max-height raised to 580px;
             zoom wrapper + controls + scroll-to-zoom added
    ══════════════════════════════════════ *@
<div class="modal fade frs-modal" id="modalRemarkImage" tabindex="-1" role="dialog" aria-hidden="true" style="z-index:1060; left:230px; right:340px; width:auto;">
    <div class="modal-dialog modal-dialog-centered" style="max-width:100%; margin:auto;">
        <div class="modal-content">

            <div class="modal-header">
                <span class="modal-title">
                    <i class="fas fa-image"></i> Remark Image
                </span>
                <button type="button" class="btn-close-frs" data-dismiss="modal" onclick="frsResetZoom()">
                    <i class="fas fa-times"></i>
                </button>
            </div>

            <div class="modal-body" style="text-align:center;padding:16px 20px 10px;">

                <div id="remarkImageLoader" style="padding:30px 0;color:#475569;">
                    <i class="fas fa-spinner fa-spin" style="font-size:22px;display:block;margin-bottom:10px;color:#3b82f6;"></i>
                    Loading image...
                </div>

                <div id="remarkImageWrap" style="display:none;">
                    <div class="frs-img-viewer-wrap" id="frsImgViewerWrap"
                         onwheel="frsHandleWheel(event)"
                         onmousedown="frsStartDrag(event)"
                         onmousemove="frsDoDrag(event)"
                         onmouseup="frsEndDrag()"
                         onmouseleave="frsEndDrag()">

                        <img id="remarkImageEl"
                             src=""
                             alt="Remark Image" />

                        <div class="frs-zoom-controls">
                            <button class="frs-zoom-btn" onclick="frsZoomOut()" title="Zoom out">
                                <i class="fas fa-minus"></i>
                            </button>
                            <span class="frs-zoom-level" id="frsZoomLevel">100%</span>
                            <button class="frs-zoom-btn" onclick="frsZoomIn()" title="Zoom in">
                                <i class="fas fa-plus"></i>
                            </button>
                            <button class="frs-zoom-reset" onclick="frsResetZoom()" title="Reset zoom">1:1</button>
                        </div>
                    </div>
                    <p class="frs-img-hint">
                        <i class="fas fa-mouse" style="margin-right:4px;"></i>Scroll to zoom &nbsp;&middot;&nbsp; Drag to pan when zoomed
                    </p>
                </div>

                <div id="remarkImageError" style="display:none;color:#f87171;font-size:13px;padding:30px 0;">
                    <i class="fas fa-exclamation-circle" style="margin-right:6px;"></i>
                    No image available for this remark.
                </div>

            </div>

            <div class="modal-footer">
                <button type="button" class="frs-btn frs-btn-cancel" data-dismiss="modal" onclick="frsResetZoom()">
                    <i class="fas fa-times"></i> Close
                </button>
            </div>

        </div>
    </div>
</div>



@* ══════════════════════════════════════
    MODAL 4 — Alert Message detail
    fnShowAlertMessage(id) writes into #spnAlertMessage
    ══════════════════════════════════════ *@
<div class="modal fade frs-modal" id="modelShowAlertMessage" tabindex="-1" role="dialog" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" style="max-width:520px;">
        <div class="modal-content">

            <div class="modal-header">
                <span class="modal-title">
                    <i class="fas fa-exclamation-triangle"></i> Alert Message Detail
                </span>
                <button type="button" class="btn-close-frs" data-dismiss="modal">
                    <i class="fas fa-times"></i>
                </button>
            </div>

            <div class="modal-body">
                <div id="spnAlertMessage"
                     style="font-size:13px;line-height:1.8;color:#cbd5e1;word-break:break-word;">
                </div>
            </div>

            <div class="modal-footer">
                <button type="button" class="frs-btn frs-btn-cancel" data-dismiss="modal">
                    <i class="fas fa-times"></i> Close
                </button>
            </div>

        </div>
    </div>
</div>


@* ══════════════════════════════════════
    IMAGE ZOOM JS — for Modal 3b
    ══════════════════════════════════════ *@
<script>
    (function () {
        var _scale = 1, _panX = 0, _panY = 0, _dragging = false, _lx = 0, _ly = 0;

        function _apply() {
            var img = document.getElementById('remarkImageEl');
            if (!img) return;
            img.style.transform = 'scale(' + _scale + ') translate(' + (_panX / _scale) + 'px, ' + (_panY / _scale) + 'px)';
            var lvl = document.getElementById('frsZoomLevel');
            if (lvl) lvl.textContent = Math.round(_scale * 100) + '%';
            var wrap = document.getElementById('frsImgViewerWrap');
            if (wrap) wrap.classList.toggle('is-zoomed', _scale > 1);
        }

        window.frsZoomIn = function () { _scale = Math.min(_scale + 0.25, 4); _apply(); };
        window.frsZoomOut = function () { _scale = Math.max(_scale - 0.25, 0.5); if (_scale <= 1) { _panX = 0; _panY = 0; } _apply(); };
        window.frsResetZoom = function () { _scale = 1; _panX = 0; _panY = 0; _apply(); };

        window.frsHandleWheel = function (e) {
            e.preventDefault();
            var d = e.deltaY > 0 ? -0.15 : 0.15;
            _scale = Math.min(Math.max(_scale + d, 0.5), 4);
            if (_scale <= 1) { _panX = 0; _panY = 0; }
            _apply();
        };
        window.frsStartDrag = function (e) {
            if (_scale <= 1) return;
            _dragging = true; _lx = e.clientX; _ly = e.clientY;
            var wrap = document.getElementById('frsImgViewerWrap');
            if (wrap) wrap.classList.add('is-dragging');
        };
        window.frsDoDrag = function (e) {
            if (!_dragging) return;
            _panX += (e.clientX - _lx); _panY += (e.clientY - _ly);
            _lx = e.clientX; _ly = e.clientY;
            _apply();
        };
        window.frsEndDrag = function () {
            _dragging = false;
            var wrap = document.getElementById('frsImgViewerWrap');
            if (wrap) wrap.classList.remove('is-dragging');
        };

        /* ─────────────────────────────────────────────────────────
           frsRenderRemarkCards(items)
           Call this from your AJAX success handler to render
           timeline cards inside #divFRSAlertRemark.

           Each item should have:
             seq        – row number
             ackType    – "PT" | "T" | "F" | "M"
             causeCode  – string (optional)
             remark     – string
             maintainer – string
             dept       – string (optional)
             rectDate   – string (date portion)
             rectTime   – string (time portion)
             remarkId   – id passed to fnShowRemarkImage (optional)
             hasImage   – boolean
        ─────────────────────────────────────────────────────────── */
        var _pillMap = {
            'PT': 'frs-pill-pt',
            'T': 'frs-pill-t',
            'F': 'frs-pill-f',
            'M': 'frs-pill-m'
        };
        var _pillLabel = { 'PT': 'PT &ndash; Partially True', 'T': 'T &ndash; True', 'F': 'F &ndash; False', 'M': 'M &ndash; Maintenance' };

        window.frsRenderRemarkCards = function (items) {
            var container = document.getElementById('divFRSAlertRemark');
            if (!container) return;
            if (!items || !items.length) {
                container.innerHTML = '<div class="frs-history-loading"><i class="fas fa-inbox"></i> No remarks found.</div>';
                return;
            }
            var html = '';
            items.forEach(function (r, idx) {
                var pillClass = _pillMap[r.ackType] || 'frs-pill-m';
                var pillLbl = _pillLabel[r.ackType] || r.ackType;
                var src = r.thumbSrc || '';

                /* blurred background style (only when image available) */
                var bgStyle = src ? ' style="background-image:url(\'' + src + '\')"' : '';

                /* sharp thumb inner */
                var thumbInner = src
                    ? '<img src="' + src + '" alt="Remark image" />'
                    : '<div class="frs-remark-img-thumb-icon"><i class="fas fa-image"></i></div>';

                /* image panel */
                var imgColClass = r.hasImage ? 'frs-remark-img-col' : 'frs-remark-img-col no-image';
                var imgColClick = r.hasImage ? ' onclick="frsImgColClick(this,' + r.remarkId + ')"' : '';
                var badgeText = r.hasImage ? '<i class="fas fa-expand-alt" style="font-size:8px"></i> View' : '<i class="fas fa-ban" style="font-size:8px"></i> None';

                var imgCol = '<div class="' + imgColClass + '"' + imgColClick + '>'
                    + '<div class="frs-remark-img-bg"' + bgStyle + '></div>'
                    + '<div class="frs-remark-img-thumb">' + thumbInner + '</div>'
                    + '<div class="frs-remark-img-badge">' + badgeText + '</div>'
                    + '</div>';

                var causeHtml = r.causeCode
                    ? '<span class="frs-remark-cause"><i class="fas fa-tag"></i>' + r.causeCode + '</span>'
                    : '';

                html += '<div class="frs-remark-card">'
                    + '<div class="frs-remark-card-head">'
                    + '<span class="frs-remark-seq">' + (idx + 1) + '</span>'
                    + '<span class="frs-pill ' + pillClass + '"><span class="dot"></span>' + pillLbl + '</span>'
                    + '<div class="frs-remark-card-head-right">'
                    + '<span class="frs-remark-meta"><i class="fas fa-user"></i><strong>' + (r.maintainer || '—') + '</strong>' + (r.dept ? ' &middot; ' + r.dept : '') + '</span>'
                    + '<span class="frs-remark-meta"><i class="fas fa-clock"></i>' + (r.rectTime || '—') + '</span>'
                    + '</div>'
                    + '</div>'
                    + '<div class="frs-remark-card-main">'
                    + imgCol
                    + '<div class="frs-remark-content-col">' + causeHtml + '<p class="frs-remark-text">' + (r.remark || '') + '</p></div>'
                    + '</div>'
                    + '<div class="frs-remark-card-foot">'
                    + '<span class="frs-remark-foot-item"><i class="fas fa-calendar-alt"></i><span>' + (r.rectDate || '—') + '</span></span>'
                    + (r.dept ? '<span class="frs-remark-foot-item"><i class="fas fa-building"></i><span>' + r.dept + '</span></span>' : '')
                    + '</div>'
                    + '</div>';
            });
            container.innerHTML = html;
        };

        /* ripple + open image */
        window.frsImgColClick = function (el, remarkId) {
            var rect = el.getBoundingClientRect();
            var ripple = document.createElement('span');
            ripple.className = 'frs-ripple';
            var size = Math.max(el.offsetWidth, el.offsetHeight);
            ripple.style.cssText = 'width:' + size + 'px;height:' + size + 'px;'
                + 'left:' + (event.clientX - rect.left - size / 2) + 'px;'
                + 'top:' + (event.clientY - rect.top - size / 2) + 'px;';
            el.appendChild(ripple);
            ripple.addEventListener('animationend', function () { ripple.remove(); });
            setTimeout(function () { fnShowRemarkImage(remarkId); }, 180);
        }

        document.addEventListener('DOMContentLoaded', function () {
            var modal = document.getElementById('modalRemarkImage');
            if (modal) {
                /* suppress Bootstrap backdrop; use our scoped pseudo-element instead */
                $(modal).data('bs.modal') && ($(modal).data('bs.modal')._config.backdrop = false);
                $(modal).on('show.bs.modal', function () {
                    /* re-apply backdrop:false each time in case modal was re-initialised */
                    var instance = $(modal).data('bs.modal');
                    if (instance) instance._config.backdrop = false;
                    document.body.classList.add('frs-image-modal-open');
                });
                $(modal).on('hidden.bs.modal', function () {
                    window.frsResetZoom();
                    document.body.classList.remove('frs-image-modal-open');
                });
            }
        });
    })();
</script>
