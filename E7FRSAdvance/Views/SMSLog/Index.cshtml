@model Domain.SMSLogLister

@{
    ViewBag.Title = "Alert Logs";
}

<script src="~/assets/js/jquery-3.5.1.min.js"></script>

@*<div class="container-fluid">
    <div class="page-title">
        <div class="row">
            <div class="col-12">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="#"> <i data-feather="home"></i></a></li>
                    <li class="breadcrumb-item active">SMS Log</li>
                </ol>
            </div>
        </div>
    </div>
</div>*@
<div class="">
    <div class="">

        @using (Ajax.BeginForm("SMSLogList", "SMSLog", new AjaxOptions()
        {
            HttpMethod = "POST",
            UpdateTargetId = "divSMSLogList",
            InsertionMode = InsertionMode.Replace,
            LoadingElementId = "loader",
            OnSuccess = "SMSLogRefreshed"
        }, new { id = "frmGetSMSLogList" }))
        {
            { HtmlHelper.ClientValidationEnabled = false; }
            <div id="divSMSLogList">
                @Html.Action("SMSLogList")
            </div>
        }
    </div>
</div>
<script src="~/Content/PageJS/SMSLog/SMSLogIndex.js"></script>