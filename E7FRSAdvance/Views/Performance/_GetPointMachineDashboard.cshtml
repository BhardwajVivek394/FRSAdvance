@model Domain.AssetLister
<style>
    .popover {
        width: 100%;
    }

    .popover-content {
        color: black;
    }

    @@media only screen and (max-width: 1200px) {
        .panel-body {
        }
    }
</style>
<div class="card">

    <div class="card-body" style="padding:15px;">

        @if (Model.mAssets != null && Model.mAssets.Count > 0)
        {
            foreach (var item in Model.mAssets)
            {
                int cnt = item.AssetTypeName.Length + item.Name.Length;

                var vibration = item.assetAttributes.Where(x => x.Title.Contains("Vibration")).ToList();
                bool isVibration = false;
                if (vibration != null && vibration.Count > 0)
                {
                    isVibration = true;
                }
                string aEnd = string.Empty;
                string bEnd = string.Empty;
                string aVibration = string.Empty;
                string bVibration = string.Empty;
                if (item.assetAttributes != null && item.assetAttributes.Count > 0)
                {
                    var mAEnd = item.assetAttributes.Where(x => x.Title == "A End - NWKR" || x.Title == "A End - RWKR").ToList();
                    if (mAEnd != null && mAEnd.Count > 0)
                    {
                        aEnd = Newtonsoft.Json.JsonConvert.SerializeObject(mAEnd);
                    }
                    var mBEnd = item.assetAttributes.Where(x => x.Title == "B End - NWKR" || x.Title == "B End - RWKR").ToList();
                    if (mBEnd != null && mBEnd.Count > 0)
                    {
                        bEnd = Newtonsoft.Json.JsonConvert.SerializeObject(mBEnd);
                    }
                    var mAVibration = item.assetAttributes.Where(x => x.Title.Contains("Vibration A")).ToList();
                    if (mAVibration != null && mAVibration.Count > 0)
                    {
                        aVibration = Newtonsoft.Json.JsonConvert.SerializeObject(mAVibration);
                    }
                    var mBVibration = item.assetAttributes.Where(x => x.Title.Contains("Vibration B")).ToList();
                    if (mBVibration != null && mBVibration.Count > 0)
                    {
                        bVibration = Newtonsoft.Json.JsonConvert.SerializeObject(mBVibration);
                    }
                }

                <div class="col-sm-12 col-md-12 col-lg-12">
                    <div class="card">
                        <div class="col-xl-12 news box-col-12">
                            <div class="card card-border">
                                <div class="bg-primary" style="padding:0;border-top-left-radius:15px;border-top-right-radius:15px;">
                                    <div class="" style="width:95%;float:left;padding:5px;border-top-left-radius:15px;">
                                        <h6 style="margin-left:10px">
                                            <a target="_blank" class="text-white" href="@item.HttpMisLink"> @item.AssetTypeName : @item.Name</a>
                                        </h6>
                                        <span style="margin-left:11px;">Index Score: @item.IndexScore</span>
                                    </div>
                                    <div class="text-center bg-dark" style="width:5%;float:left;padding:2px 0px;border-top-right-radius:15px;line-height:1.3;height:58px">

                                        @if (item.UnderMaintence == true)
                                        {
                                            <div class="text-center"> <i class="fa fa-bell-slash fa fa-das" style="font-size:16px;cursor:pointer;" onclick="fnUnderMaintenance('@item.Id', this)" data-id="@item.Id"></i></div>
                                        }
                                        else
                                        {
                                            <div class="text-center">
                                                <i class="fa fa-bell" style="font-size:16px;cursor:pointer;" onclick="fnUnderMaintenance('@item.Id', this)" data-id="@item.Id"></i>
                                            </div>
                                        }
                                        @*<a onclick="fnGetAssetGraph('@item.SiteId','@item.Id')" style="color:white;text-transform:capitalize;font-size:10px;display:flow-root;cursor:pointer"> Graph </a>*@
                                        <a onclick="fnGetSmsLog('@item.Id')" style="color:white;text-transform:capitalize;font-size:10px;display:flow-root;cursor:pointer">Event log</a>
                                    </div>

                                </div>
                                <div class="card-body" style="padding:10px;margin-bottom:-15px">
                                  
                                    <div class="table-responsive width100p tblAssetData">
                                    </div>
                                    <div class="table-responsive width100p tblAssetData m-b-20" style="overflow:auto;max-height:420px;">

                                        <table class="table table-bordered table-striped" id="tblPoint@(item.Id)">
                                            <thead>
                                                <tr>
                                                    <th>Date</th>
                                                    <th>Name</th>
                                                    <th>Direction</th>
                                                    @if (item.IsSeriesOpration != null && item.IsSeriesOpration.Value)
                                                    {
                                                        <th>Combine</th>
                                                    }

                                                    @if (item.IsHalfPointMachine == null || (item.IsHalfPointMachine != null && !item.IsHalfPointMachine.Value))
                                                    {

                                                        <th>A Current(Max/Avg)</th>
                                                        <th>A Voltage</th>
                                                        <th>A Time (ms)</th>
                                                    }
                                                    else
                                                    {
                                                        <th>Current(Max/Avg)</th>
                                                        <th>Voltage</th>
                                                        <th>Time (ms)</th>
                                                    }


                                                    @if (item.IsHalfPointMachine == null || (item.IsHalfPointMachine != null && !item.IsHalfPointMachine.Value))
                                                    {
                                                        <th>B Current(Max/Avg)</th>
                                                        <th>B Voltage</th>
                                                        <th>B Time (ms)</th>
                                                    }

                                                </tr>
                                            </thead>
                                            <tbody class="fbody@(item.Id)">
                                                @Html.Partial("_PointMachineDatePartial", item)
                                            </tbody>
                                        </table>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            }
        }
        else
        {
            <p style="font-size:15px">No record found!</p>
        }



    </div>
</div>

<div class="modal bounceIn animated" id="modal-graph" tabindex="-1" role="dialog" data-bs-backdrop="static">
    <div class="modal-dialog modal-width modal-xl">
        <div class="modal-content bradius4">
            <div class="modal-header">
                <h6 class="modal-title" id="myModalLabel">
                    <span id="line1">52 2021-05-26 17:07:00 <b id="op">Normal Operation:</b></span><br />
                    <span id="line2">- A Max / Average Current: 3.8 / 2.34</span><br />
                    <span id="line3">- A Max / Average Voltage: 118.9 / 116.00</span><br />
                    <span id="line4">- A Operating Time (ms): 3300</span><br />
                    <span id="line5">B Max / Average Current: null / 0.0</span><br />
                    <span id="line6">- B Max / Average Voltage: 0 / 0.00</span><br />
                    <span id="line7">- B Operating Time (ms): 0</span><br />
                </h6>
                <button type="button" style="position:absolute;right:8px;top:8px;" class="btn-close" data-bs-dismiss="modal" aria-hidden="true"></button>
            </div>
            <div class="modal-body">
                <div class="row">
                    <div class="col-sm-12 mainclass" id="mainId">
                        <canvas id='basicflot' style='height: 263px'></canvas>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button class="btn btn-default" data-bs-dismiss="modal" id="btnClose">Close</button>
            </div>
        </div>
    </div>
</div>

@{
    var serializer = new System.Web.Script.Serialization.JavaScriptSerializer();
    serializer.MaxJsonLength = Int32.MaxValue;
    var jsonModel = serializer.Serialize(Model);
}
<script src="https://cdn.jsdelivr.net/npm/chart.js@2.9.4/dist/Chart.min.js"></script>
<script src="~/assets/js/chart/echart/echarts.js"></script>
<script>
    var json = "";
    var client = null;
    var mQQTPath = "";
    var channels = [];
    var aEN = 0;
    var aER = 0;
    var bEN = 0;
    var bER = 0;
    (function ($) {
        $.fn.focusTextToEnd = function () {
            this.focus();
            var $thisVal = this.val();
            this.val('').val($thisVal);
            return this;
        }
    }(jQuery));
    $(document).ready(function () {
        $(".asset-search").keyup(function (e) {
            setTimeout(function () {
                $("#frmAssetDetails").submit();
            }, 3000)
        });
        $('[data-toggle="popover"]').popover();

        $("#lblAssetType").html($("#AssetName" + $("#hdnassetTypeId").val()).val());
        $(".asset-search").focusTextToEnd();
     //   $('#Pager').select2({ minimumResultsForSearch: Infinity });

     //   $('#drpPageSize').select2({ minimumResultsForSearch: Infinity });

        $('#drpPageSize').change(function (e) {
            $("#hdnSkip").val('0');
            //$("#hdndateValue").val();
            //$("#hdnassetTypeId").val();
            $("#frmAssetDetails").submit();
        });

        $('.from-date').datepicker();
        $('.to-date').datepicker();


        $('#txtFromDate').change(function () {
            fnShowPointMachineData();
        });

        $('#txtToDate').change(function () {
            fnShowPointMachineData();
        });
        fnShowPointMachineData();


    });

    function fnShowPointMachineData() {
        var start = new Date($('#txtFromDate').val());
        var end = new Date($('#txtToDate').val());
        var days = new Date(end - start) / (1000 * 60 * 60 * 24);
        if (Math.round(days) >= 1) {
            $('#SearchCriteria_TakePointMachineData').show();
        } else {
            $("#SearchCriteria_TakePointMachineData").val($("#SearchCriteria_TakePointMachineData option:first").val());
            $('#SearchCriteria_TakePointMachineData').hide();
        }
    }

    function GetPagerButtonClick(btn) {
        if (btn == "Next") {
            if (@Model.Pager.TotalPage != @Model.Pager.CurrentPage) {
                var hdnSkip = $("#hdnSkip").val();
                var pageSize = @Model.Pager.PageSize;
                var skip = parseInt(hdnSkip) + parseInt(pageSize);
                $("#hdnSkip").val(skip);
                $("#frmAssetDetails").submit();
            }
        }
        else if (btn == "Previous") {
            if (@Model.Pager.Skip >= @Model.Pager.PageSize)
            {
                var skip =@Model.Pager.Skip;
                skip = skip - @Model.Pager.PageSize;
                $("#hdnSkip").val(skip);
                $("#frmAssetDetails").submit();
            }
        }
    }

    function Clear(id) {
        $("#txtFromDate" + id).val('');
        $("#txtToDate" + id).val('');
        $("#frmAssetDetails").submit();
    }

    function stringToDate(s) {
        var ret = NaN;
        var parts = s.split("/");
        date = new Date(parts[2], parts[0], parts[1]);
        if (!isNaN(date.getTime())) {
            ret = date;
        }
        return ret;
    }

    function stringtableToDate(s) {
        var ret = NaN;
        var parts = s.split("-");
        date = new Date(parts[2], parts[0], parts[1]);
        if (!isNaN(date.getTime())) {
            ret = date;
        }
        return ret;
    }

    function Search(assetId) {

        $("#frmAssetDetails").submit();
        //var pointMachineData = {};
        //pointMachineData.AssetId = assetId;
        //if ($("#txtFromDate" + assetId).val() != "") {
        //    pointMachineData.StartDate = $("#txtFromDate" + assetId).val();
        //}
        //else {
        //    pointMachineData.StartDate = new Date();
        //}
        //if ($("#txtToDate" + assetId).val() != "") {
        //    pointMachineData.EndDate = $("#txtToDate" + assetId).val();

        //}
        //else {
        //    pointMachineData.EndDate = new Date();
        //}

        //$("#loader").show();
        //$.ajax({
        //    url: '/Site/GetPointMachineDate',
        //    type: 'Post',
        //    data: '{pointMachineData:' + JSON.stringify(pointMachineData) + '}',
        //    dataType:'html',
        //    contentType: 'application/json',
        //    success: function (data) {
        //        $("#loader").hide();
        //        $(".fbody" + assetId).empty().append(data);
        //    },
        //    error: function (response) {
        //        $("#loader").hide();
        //        CommonNotification("ERROR", "Something went wrong!", "Error");
        //    }
        //});
    }

    function DownloadPointData(id) {
        var titles = [];
        var data = [];
        var tbl = "#tblPoint" + id + " > thead > tr > th"
        var tblbody = ".fbody" + id + " > tr";
        $(tbl).each(function () {
            titles.push($.trim($(this).text()));
        });

        //$('#tbodyExportOrder > tr > td').each(function () {
        //       data.push($.trim($(this).text()));
        //});

        $(tblbody).each(function (id, ele) {
            $(this).find('td').each(function (id, ele) {
                var datas = $.trim($(ele).text());
                data.push($.trim(datas));
            });
        });

        var csvString = prepCSVRow(titles, titles.length, '');
        csvString = prepCSVRow(data, titles.length, csvString);
        var downloadLink = document.createElement("a");
        var blob = new Blob(["\ufeff", csvString]);
        var url = URL.createObjectURL(blob);
        downloadLink.href = url;
        downloadLink.download = "PointMachineData" + GenerateUniqueCode() + ".csv";

        document.body.appendChild(downloadLink);
        downloadLink.click();
        document.body.removeChild(downloadLink);
    }

    function prepCSVRow(arr, columnCount, initial) {
        var row = '';
        var delimeter = ',';
        var newLine = '\r\n';

        function splitArray(_arr, _count) {
            var splitted = [];
            var result = [];
            _arr.forEach(function (item, idx) {
                if ((idx + 1) % _count === 0) {
                    splitted.push(item);
                    result.push(splitted);
                    splitted = [];
                } else {
                    splitted.push(item);
                }
            });
            return result;
        }
        var plainArr = splitArray(arr, columnCount);
        plainArr.forEach(function (arrItem) {
            arrItem.forEach(function (item, idx) {
                row += item + ((idx + 1) === arrItem.length ? '' : delimeter);
            });
            row += newLine;
        });
        return initial + row;
    }

    function GenerateUniqueCode() {
        var currentdate = new Date();
        return ((currentdate.getTime() * 10000) + 621355968000000000);
    }

    function fnShowMultipleGraph(assetId, datevalue ,type) {
        $.ajax({
            url: '/Site/GetSitePointMachineGraph',
            type: 'Post',
            data: '{assetId:' + assetId + ',take:' + $('#SearchCriteria_TakePointMachineData').val() + ',type:"' + datevalue + '",dt:"' + $('#txtFromDate').val() + '",edt:"' + $('#txtToDate').val() + '"}',
            contentType: 'application/json',
            success: function (data) {
                $("#loader").hide();
                fnBindMultipleSignatureGragh(data, type, datevalue);

            },
            error: function (response) {
                $("#loader").hide();
                CommonNotification("ERROR", "Internal server error.", "error");
            }
        });

    }

    function fnShowMultipleGraphBySite(siteId, type, datevalue) {
        $("#loader").show();
        $.ajax({
            url: '/Site/GetPointMachineDataBySiteId',
            type: 'Post',
            data: '{siteId:' + siteId + ',take:' + $('#SearchCriteria_TakePointMachineData').val() + ',type:"' + datevalue + '",dt:"' + $('#txtFromDate').val() + '",edt:"' + $('#txtToDate').val() + '"}',
            contentType: 'application/json',
            success: function (data) {
                $("#loader").hide();
                fnBindMultipleSignatureGraghBySite(data, type, datevalue, siteId);

            },
            error: function (response) {
                $("#loader").hide();
                CommonNotification("ERROR", "Internal server error.", "error");
            }
        });

    }

    function fnChkPointMachineGraph(siteId, type, datevalue) {
        var assetIds = [];
        var datasets = [];
        var label = [];
        var chkLength = $('.chkPointMachineGraph:checkbox:checked').length;
        var counter = 0;
        $('.chkPointMachineGraph:checkbox:checked').each(function () {
            assetIds.push(parseInt($(this).val()));

            $("#loader").show();
            $.ajax({
                url: '/Site/GetSitePointMachineGraph',
                type: 'Post',
                data: '{assetId:' + $(this).val() + ',take:' + $('#SearchCriteria_TakePointMachineData').val() + ',type:"' + datevalue + '",dt:"' + $('#txtFromDate').val() + '",edt:"' + $('#txtToDate').val() + '"}',
                contentType: 'application/json',
                success: function (data) {
                    $("#loader").hide();
                    counter++;
                    if (chart != null && chart != "" && chart != undefined) {
                        chart.dispose();
                    }

                    var isValid = true;

                    var assetName = [];
                    if (data != null && data != undefined && data != '') {
                        var legends = '';
                        var lineColor = '';

                        $(data).each(function (data, val) {
                            var dsd = assetIds.indexOf(val.AssetId);
                            if (assetIds.indexOf(val.AssetId) >= 0) {
                                var array = [];
                                if (val.AssetType != legends) {
                                    lineColor = fnGetDarkColor();
                                }
                                const d = new Date(val.Date);
                                //legends = val.AssetType + " (" + d.getDate() + ")";
                                legends = val.AssetType + "|" + d.getHours() + ":" + d.getMinutes() + "";
                                assetName.push(val.AssetType);
                                var j = val.PointMachineJson;
                                // var array = [];
                                var labels = "";
                                var total = 0;

                                if (type == "CC") {
                                    labels = "Combine C";
                                    $.each(j.A_C, function (id, val) {
                                        if (val != null && val != undefined && val != '') {
                                            var num = parseInt(val);
                                            total += num;
                                            array.push(val);
                                        }                                       
                                    });

                                    $.each(j.B_C, function (id, val) {
                                        if (val != null && val != undefined && val != '') {
                                            var num = parseInt(val);
                                            total += num;
                                            array.push(val);
                                        }
                                      
                                    });
                                }

                                if (type == "CV") {
                                    labels = "Combine V";
                                    $.each(j.A_V, function (id, val) {
                                        if (val != null && val != undefined && val != '') {
                                            var num = parseInt(val);
                                            total += num;
                                            array.push(val);
                                        }
                                       
                                    });

                                    $.each(j.B_V, function (id, val) {
                                        if (val != null && val != undefined && val != '') {
                                            var num = parseInt(val);
                                            total += num;
                                            array.push(val);
                                        }                                    
                                    });
                                }

                                if (type == "AC") {
                                    labels = "A End";
                                    $.each(j.A_C, function (id, val) {
                                        if (val != null && val != undefined && val != '') {
                                            var num = parseInt(val);
                                            total += num;
                                            array.push(val);
                                        }                                       
                                    });
                                }

                                if (type == "BC") {
                                    labels = "B End";
                                    $.each(j.B_C, function (id, val) {
                                        if (val != null && val != undefined && val != '') {
                                            var num = parseInt(val);
                                            total += num;
                                            array.push(val);
                                        }                                       
                                    });
                                }


                                var marker = total / array.length;
                                var time = j.A_C_TIME;
                                var len = parseInt(j.A_C_TIME) / parseInt(1000);
                                var loop = parseInt(array.length / 2) + 1;

                                for (var i = 0; i <= array.length; i++) {
                                    var xVal = (i) * (100);
                                    if (label.indexOf(xVal) == -1) {
                                        label.push(xVal);
                                    }
                                    //label.push(xVal);
                                }

                                var ds = {
                                    label: legends,
                                    fill: false,
                                    lineTension: 0.1,
                                    backgroundColor: "rgba(75,192,192,0.4)",
                                    borderColor: lineColor,
                                    borderCapStyle: 'butt',
                                    borderDash: [],
                                    borderDashOffset: 0.0,
                                    borderJoinStyle: 'miter',
                                    pointBorderColor: lineColor,
                                    pointBackgroundColor: "#fff",
                                    pointBorderWidth: 0.5,
                                    pointHoverRadius: 5,
                                    pointHoverBackgroundColor: lineColor,
                                    pointHoverBorderColor: "rgba(220,220,220,1)",
                                    pointHoverBorderWidth: 1,
                                    pointRadius: 5,
                                    pointHitRadius: 10,
                                    data: array,
                                    markLine: {
                                        data: [{
                                            type: 'average',
                                            name: '???'
                                        }]
                                    }
                                };
                                datasets.push(ds);
                            }


                        });

                    }
                    if (chkLength == counter) {
                        var pieChartContent = document.getElementById('pointmachine-mainId');
                        pieChartContent.innerHTML = '&nbsp;';
                        $('#pointmachine-mainId').append('<canvas id="pointmachine-basicflot"style="height: 263px"><canvas>');

                        var canvas = $("#pointmachine-basicflot").get(0).getContext("2d");

                        // do some drawing

                        var data = {
                            labels: label,
                            datasets: datasets
                        };

                        var option = {
                            showLines: true,

                        };
                        var myLineChart = Chart.Line(canvas, {
                            data: data,
                            options: option
                        });
                    }


                },
                error: function (response) {
                    $("#loader").hide();
                    CommonNotification("ERROR", "Internal server error.", "error");
                }
            });
        });



    }

    function fnBindSignatureGragh(data, type, datevalue, direction, name) {
        $("div").remove(".chartjs-size-monitor");       
        var j = JSON.parse(data);
        var acg;
        var bcg;
        var avg;
        var bvg;
        if (j.A_C_AVERAGE != null) {
            acg = parseFloat(j.A_C_AVERAGE).toFixed(2);
        }
        if (j.B_C_AVERAGE != null) {
            bcg = parseFloat(j.B_C_AVERAGE).toFixed(2);
        }
        if (j.A_V_AVERAGE != null) {
            avg = parseFloat(j.A_V_AVERAGE).toFixed(2);
        }
        if (j.B_V_AVERAGE != null) {
            bvg = parseFloat(j.B_V_AVERAGE).toFixed(2);
        }

        $("#line1").empty().text(name + " " + datevalue);
        $("#op").text(direction + " Operation");
        $("#line2").empty().text("- A Max / Average Current:" + j.A_C_MAX + "/" + acg);
        $("#line3").empty().text("-  Average Voltage:" + avg);
        $("#line4").empty().text("- A Operating Time (ms):" + j.A_C_TIME);
        $("#line5").empty().text("- B Max / Average Current:" + j.B_C_MAX + "/" + bcg);
        $("#line6").empty().text("- Average Voltage:" + bvg);
        $("#line7").empty().text("- B Operating Time (ms):" + j.B_C_TIME);

        var array = [];
        var labels = "";
        var total = 0;
        if (type == "CC") {
            labels = "Combine C";
            $.each(j.A_C, function (id, val) {
                if (val != null && val != undefined && val != '') {
                    var num = parseInt(val);
                    total += num;
                    array.push(val);
                }
            });

            $.each(j.B_C, function (id, val) {
                if (val != null && val != undefined && val != '') {
                    var num = parseInt(val);
                    total += num;
                    array.push(val);
                }
            });
        }

        if (type == "CV") {
            labels = "Combine V";
            $.each(j.A_V, function (id, val) {
                if (val != null && val != undefined && val != '') {
                    var num = parseInt(val);
                    total += num;
                    array.push(val);
                }
            });

            $.each(j.B_V, function (id, val) {
                if (val != null && val != undefined && val != '') {
                    var num = parseInt(val);
                    total += num;
                    array.push(val);
                }
            });
        }

        if (type == "AC") {
            labels = "A End";
            $.each(j.A_C, function (id, val) {
                if (val != null && val != undefined && val != '') {
                    var num = parseInt(val);
                    total += num;
                    array.push(val);
                }
            });
        }
        if (type == "BC") {
            labels = "B End";
            $.each(j.B_C, function (id, val) {
                if (val != null && val != undefined && val != '') {
                    var num = parseInt(val);
                    total += num;
                    array.push(val);
                }

            });
        }
        if (type == "AV") {
            labels = "A End";
            $.each(j.A_V, function (id, val) {
                if (val != null && val != undefined && val != '') {
                    var num = parseInt(val);
                    total += num;
                    array.push(val);
                }
            });
        }
        if (type == "BV") {
            labels = "B End";
            $.each(j.B_V, function (id, val) {
                if (val != null && val != undefined && val != '') {
                    var num = parseInt(val);
                    total += num;
                    array.push(val);
                }
            });
        }

        var marker = total / array.length;
        var time = j.A_C_TIME;
        var len = parseInt(j.A_C_TIME) / parseInt(1000);
        var loop = parseInt(array.length / 2) + 1;

        var label = [];

        for (var i = 0; i <= array.length; i++) {
            var xVal = (i) * (100);
            label.push(xVal);
        }


        var pieChartContent = document.getElementById('mainId');
        pieChartContent.innerHTML = '&nbsp;';
        $('#mainId').append('<canvas id="basicflot"style="height: 263px"><canvas>');

        var canvas = $("#basicflot").get(0).getContext("2d");

        // do some drawing

        var data = {
            labels: label,
            datasets: [
                {
                    label: labels + " Current",
                    fill: false,
                    lineTension: 0.1,
                    backgroundColor: "rgba(75,192,192,0.4)",
                    borderColor: "rgba(75,192,192,1)",
                    borderCapStyle: 'butt',
                    borderDash: [],
                    borderDashOffset: 0.0,
                    borderJoinStyle: 'miter',
                    pointBorderColor: "rgba(75,192,192,1)",
                    pointBackgroundColor: "#fff",
                    pointBorderWidth: 0.5,
                    pointHoverRadius: 5,
                    pointHoverBackgroundColor: "rgba(75,192,192,1)",
                    pointHoverBorderColor: "rgba(220,220,220,1)",
                    pointHoverBorderWidth: 1,
                    pointRadius: 5,
                    pointHitRadius: 10,
                    data: array,
                    markLine: {
                        data: [{
                            type: 'average',
                            name: '???'
                        }]
                    }
                }
            ]
        };

        var option = {
            showLines: true,

        };
        var myLineChart = Chart.Line(canvas, {
            data: data,
            options: option
        });
        $("#modal-graph").modal("show");
    }
</script>
