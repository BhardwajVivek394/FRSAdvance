@model  List<Domain.Asset>
@{
    List<string> types = new List<string>();
}
<table class="table">
    <thead>
        <tr>
            <th>Station</th>
            @if (Model != null && Model.Count > 0)
            {
                foreach (var item in Model.GroupBy(x => x.AssetTypeName).ToList())
                {
                    <th>@item.Key</th>

                    types.Add(item.Key);
                }
            }

        </tr>
    </thead>
    <tbody>
        @if (Model != null && Model.Count > 0)
        {
            foreach (var item in Model.GroupBy(x => x.SiteName).ToList())
            {
                <tr>
                    <td>@item.Key</td>
                    @foreach (var type in types)
                    {
                        var smsCount = Model.Where(x => x.AssetTypeName == type && x.SiteName == item.Key).Count();
                        <td>@smsCount</td>
                    }

                </tr>
            }

        }

    </tbody>
</table>

