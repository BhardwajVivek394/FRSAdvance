@model Domain.BlogLister

@{
    ViewBag.Title = "Blog";
}
@*@Html.HiddenFor(m => m.SearchCriteria.ColumnName, new { id = "hdnColumnName" })
    @Html.HiddenFor(m => m.SearchCriteria.SortDirection, new { id = "hdnSortDirection" })*@

<script src="~/assets/js/jquery-3.5.1.min.js"></script>
<div class="container-fluid">
    <div class="page-title">
        <div class="row">
            <div class="col-12">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="#"> <i data-feather="home"></i></a></li>
                    <li class="breadcrumb-item active">Blog</li>
                </ol>
            </div>
        </div>
    </div>
</div>


<div class="">
    <div class="">
        @using (Ajax.BeginForm("GetAllBlogLister", "Blog", new AjaxOptions()
        {
            HttpMethod = "POST",
            UpdateTargetId = "divBlogList",
            InsertionMode = InsertionMode.Replace,
            LoadingElementId = "loader",
        }, new { id = "frmGetDivisionList" }))
        {
            { HtmlHelper.ClientValidationEnabled = false; }
            <div id="divBlogList">
                @Html.Action("GetAllBlogLister", Model);
            </div>
        }


        @*@using (Ajax.BeginForm("SaveDivision", "Division", new AjaxOptions()
            {
                HttpMethod = "POST",
                UpdateTargetId = "divSaveDivision",
                InsertionMode = InsertionMode.Replace,
                LoadingElementId = "loader",
                OnSuccess = "DivisionSaved"
            }, new { id = "frmSaveDivision" }))
            {
                <div id="divSaveDivision">
                    @Html.Partial("_AddDivisionPartial", new Domain.Division())
                </div>
            }*@

    </div>
</div>
@*<script src="~/Content/PageJS/Division/DivisionIndex.js"></script>*@
